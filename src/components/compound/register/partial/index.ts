import { Content } from '@/components/content';
import { Wrapper } from '@/components/wrapper';
import { Root } from '@radix-ui/react-slot';

export const UIRegisterForm = {
  Root: Root,
  Wrapper: Wrapper,
  Content: Content,
};
